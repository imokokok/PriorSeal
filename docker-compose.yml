services:
  api:
    build: .
    env_file: .env
    ports:
      - "3000:3000"
    read_only: true
    tmpfs: ["/tmp"]
    security_opt: ["no-new-privileges:true"]
    depends_on:
      postgres:
        condition: service_healthy
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: runproof
      POSTGRES_PASSWORD: development-only-change-me
      POSTGRES_DB: runproof
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U runproof -d runproof"]
      interval: 5s
      timeout: 3s
      retries: 10
    volumes:
      - postgres-data:/var/lib/postgresql/data
volumes:
  postgres-data:
